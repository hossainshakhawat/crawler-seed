# crawler-seed configuration
# All values can be overridden by environment variables prefixed with SEED_
# e.g. SEED_KAFKA_BROKER, SEED_DEPTH, SEED_SEEDS (comma-separated)

kafka_broker: "localhost:9092"
depth:        0
seeds:
  - "https://example.com"
